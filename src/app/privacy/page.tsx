// /privacy is the canonical URL used by Google Play Store.
// Re-export the privacy policy page directly so it returns 200 (not a redirect).
export { default, metadata } from "../privacy-policy/page";
